# Text Similarity Analysis

This project compares the similarity between a main document (`A.txt`) and a set of other documents using TF-IDF and cosine similarity.

## Features
- Generates sample documents.
- Calculates document similarity.

## Requirements
- Python 3.x
- scikit-learn

## Usage
1. Clone the repository.
2. Run the script in your Python environment.
